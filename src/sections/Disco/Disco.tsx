import React, { FC } from 'react';

const Disco: FC = () => {
    return (
        <section className="disco">
            <h3 className="disco_center">Дискотека</h3>
            <p className="every_friday">Каждую пятницу и субботу</p>
        </section>
    );
};

export default Disco;